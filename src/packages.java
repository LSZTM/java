public class packages {
    //java packages are containers for user classes, they can be prebuilt in jdk like java.lang,java.util or java.io etc
    //user defined packages have syntax:- package NameOfPackage ---eg
    //packages are useful in preventing naming conflicts of contents b/w one pacakage and the other packages
    //packages are small private areas which prevents naming cinflicts
}
